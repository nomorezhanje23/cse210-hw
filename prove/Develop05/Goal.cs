 using System;

namespace Developer05
{
    public class Goal
    {
        public bool _goalComplete;
        protected  string _goalName;
        protected string _goalDescription;

        protected int _points;

        protected int _goal;

         public Goal(string _goalName, string _goalDescription, int _points, bool _goalComplete)
         { 
         }

         public void SetgoalComplete(string goalComplete )
         {
         }

    }
}
